import styled from 'styled-components';

const AppWrapper = styled.div`
    min-height: 100vh;
    width: 100%;
`

function App() {
    return (
        <AppWrapper>
            
        </AppWrapper>
    );
}

export default App;
